import { makeStyles, createStyles } from '@mui/styles';

const useStyles = makeStyles(() =>
  createStyles({
    chartContainer: {
      width: '100%',
      height: '400px',
    },
  })
);

export default useStyles;